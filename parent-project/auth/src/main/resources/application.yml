keycloak:
  auth-server-url: http://localhost:8081/realms/demo
  realm: demo
  resource: spring-app
  credentials:
    secret: aB5hkZVuTYa9h96dxx1oeabUXR47jYKw
  bearer-only: true
  use-resource-role-mappings: true
  main:
    server-url: http://localhost:8081
    realm: master
    clientId: admin-cli
    username: admin
    password: admin



spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/flm_project
    username: postgres
    password: 1
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8081/realms/demo


