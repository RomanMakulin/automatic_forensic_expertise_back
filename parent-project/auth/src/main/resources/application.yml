spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/flm_project
    username: postgres
    password: 1
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8081/realms/demo

# KEYCLOAK CONFIG
keycloak:
  auth-server-url: http://localhost:8081/realms/demo
  realm: demo
  resource: spring-app
  credentials:
    secret: BfL2nXeRB65Qj80Wmfb9mtjo9GZHSahn
  bearer-only: true
  use-resource-role-mappings: true
  main:
    server-url: http://localhost:8081
    realm: master
    clientId: admin-cli
    username: admin
    password: admin

server:
  port: 8095

cors:
  allowed-origins:
    - http://localhost:8010
    - http://localhost:8090

services:
  notification: http://localhost:8087
  auth: http://localhost:8095
  frontend: http://localhost:8010
  profile: http://localhost:8090

paths:
  notification:
    send-mail: ${services.notification}/api/notification/send
    public-send-mail: ${services.notification}/api/public-notification/send
  auth:
    reset-request: ${services.auth}/api/auth/password_recovery
    verification-request: ${services.auth}/api/auth/verify-email
  frontend:
    recovery-request: ${services.frontend}/password_recovery
    login: ${services.frontend}/login
  profile:
    get-all-profiles: ${services.profile}/api/profile
    get-unverified-profiles: ${services.profile}/api/profile/unverified
    verify-profile: ${services.profile}/api/profile/verify
    cancel-validation: ${services.profile}/api/profile/cancel-validation